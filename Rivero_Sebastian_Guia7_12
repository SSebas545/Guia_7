#include <stdio.h>

int main()
{
    int i, contador = 0;
    for (i = 1; i <= 100; i++)
    {
        if (i % 2 == 0)
        {
            contador++;
        }
    }
    printf("Cantidad de pares entre 1 y 100: %d\n", contador);
    return 0;
}
