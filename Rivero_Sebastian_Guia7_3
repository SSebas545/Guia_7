#include <stdio.h>

int main()
{
    float n1, n2, n3, n4, promedio;
    printf("Ingrese 4 notas: ");
    scanf("%f %f %f %f", &n1, &n2, &n3, &n4);
    promedio = (n1 + n2 + n3 + n4) / 4.0;
    printf("Promedio: %.2f\n", promedio);
    return 0;
}
